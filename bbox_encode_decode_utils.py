import numpy as np

from utils import convert_coordinates, compute_iou, compute_greedy_nms


def encode(self):
    '''convert ground truth box into bounding box in relation to
        the default box, as in: (Δcx, Δcy, Δw, Δh)
    '''
    pass


def decode(self,):
    '''decode prediction for inference
    
    '''
    
    return
